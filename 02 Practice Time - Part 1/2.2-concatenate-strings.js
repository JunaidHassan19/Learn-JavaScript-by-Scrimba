let name = "Linda";
let greeting = "Hi there";

// Create a function that logs out "Hi there, Linda!" when called
function greet() {
  console.log(greeting + ", " + name + "!"); // Concatenating the greeting and name variables
}

greet(); // Calling the function to log the greeting
